import { OptionsSchema } from '@seiue/babel-plugin-react-intl/dist/options';
export declare type ExtractCLIOptions = Omit<OptionsSchema, 'overrideIdFn'> & {
    outFile?: string;
    idInterpolationPattern?: string;
    printMessagesToStdout?: boolean;
};
export default function extract(files: readonly string[], { outFile, idInterpolationPattern, ...extractOpts }: ExtractCLIOptions): Promise<Record<string, any[]>>;
